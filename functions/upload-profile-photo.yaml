version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:31112
functions:
  upload-profile-photo:
    lang: node18
    handler: ./upload-profile-photo
    image: julienfls/upload-profile-photo:latest
    secrets:
      - secret-pg
